{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Chrome",
            "type": "chrome",
            "request": "launch",
            "url": "http://localhost:5000", // create-react-app's default port 3000
            "webRoot": "${workspaceRoot}/library/src",
            "preLaunchTask": "npm: start" // Add prelaunch Task npm: start (defined in tasks.json)
        },
    ]
}
